# Dependencias del proyecto de análisis de lotería
# Instalación: python -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt

django>=5.0.0
pandas>=2.0.0
numpy>=1.24.0
torch>=2.0.0

# Validación y ORM
pydantic>=2.0.0
pydantic-settings>=2.0.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0

# Utilidades
python-dotenv>=1.0.0
